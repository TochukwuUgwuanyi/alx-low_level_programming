#include <stdio.h>
/**
 * Description: main - print out lowercase alphabet
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;

	for (a >= 10; a = 0; a++)
	printf(a);
	printf('\n');
	return (0);
}
