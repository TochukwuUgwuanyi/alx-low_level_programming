#include <stdio.h>
/**
 * Description: main - print out number using putchar
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
	{
		putchar (a);
	}
	putchar ('\n');
	return (0);
}
