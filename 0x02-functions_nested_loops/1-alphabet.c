#include "main.h"

/**
 * Description: main - prints the alphabet, in lowercase using 
 *
 * Return: Always 0 (success).
 */

void print_alphabet(void)

{
	int i;

	for (i = 'a'; i <= 'z'; i++)
	_putchar (i);
	_putchar ('\n');
}
